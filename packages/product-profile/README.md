# @proofscript/product-profile

Internal product-identity helper for the current ProofScript release line.

It centralizes the current ProofScript reference/profile and validates product-facing project/certificate metadata. Historical Core and Lean compatibility metadata remains separate and is not relabeled as the current language version.

This package is outside the trusted kernel boundary.

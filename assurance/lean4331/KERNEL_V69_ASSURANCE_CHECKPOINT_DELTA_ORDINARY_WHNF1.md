# ProofScript Kernel v69 Assurance Checkpoint — Delta + Ordinary WHNF 1

**Profile:** `KERNEL-universe-conformance1`  
**Core format:** 69  
**Lean baseline:** 4.33.1 / `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`

## Result

This checkpoint closes the next ordinary definitional-equality slice without
changing v69 kernel semantics.

### Machine-checked formal results

`DeltaTransparency.lean` proves, under an exact translated environment projection:

1. PS transparent-delta steps imply the corresponding Lean delta step;
2. Lean delta on a translated constant has a corresponding PS body with the exact translated result;
3. PS and Lean agree on the `none`/stuck opacity boundary.

`ReductionOrdinary.lean` then proves Core→Lean preservation for one-step and
finite-chain ordinary WHNF computation generated by:

- beta;
- zeta;
- transparent delta;
- recursive application-head reduction.

The formal modules compile on the pinned Lean executable with no `sorryAx`.

### Exact executable evidence

| Campaign | ProofScript v69 | Lean 4.33.1 |
|---|---:|---:|
| delta/transparency | 1,000 / 0 failures | 1,000 / 0 failures |
| transparent-head ordinary WHNF | 1,000 / 0 failures | 1,000 / 0 failures |

The delta campaign covers regular definitions, abbreviations, opaque declarations
and theorem opacity. The ordinary-WHNF campaign specifically exercises unfolding
a transparent function head, reattaching caller arguments, and continuing through
beta reduction; opaque heads remain stuck.

Every exact-Lean term in these two campaigns is also passed through
`Lean.Meta.checkWithKernel`.

## Important boundary

The delta environment lookup deliberately returns the already universe-instantiated
body. Therefore this checkpoint proves the logical transparency rule, **not yet**
the declaration-installation/universe-substitution implementation theorem. That
bridge belongs to O-DECL and remains open.

Likewise, this is not full definitional equality. Eta, proof irrelevance,
projections, recursors, quotients and broader contextual/congruence closure remain.

## Current classification

- O-TYP: `PARTIAL_PROVED_DIRECT_CORE`
- O-DEF: `PARTIAL_PROVED_BETA_ZETA_DELTA_ORDINARY_WHNF`
- Whole-kernel K3 equivalence: **IN PROGRESS — NOT YET CLAIMED**

## Next

The next ordinary conversion work is function eta followed by proof irrelevance.
After those rules are related, return immediately to the conversion-dependent
basic typing theorem before entering projection/recursor/quotient correspondence.

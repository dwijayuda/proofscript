const fs=require("node:fs"), path=require("node:path");
module.exports={manifest:{name:"example-proofscript-backend-names",version:"0.0.1",pluginApi:1,proofscriptReference:"v0.1",semanticBaseline:"lean-4.33.1",kinds:["backend"],requiresHostCapabilities:["backend:v1"],logicalContribution:"none"},setup(api){api.registerBackend({name:"names",build(input){fs.mkdirSync(path.dirname(input.outPath),{recursive:true});fs.writeFileSync(input.outPath,input.module.declarations.map(d=>d.name).join("\n")+"\n");return{target:"names",files:[input.outPath],executionCorrespondence:"not-claimed"};}});}};

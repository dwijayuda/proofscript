def choose(P: Prop)[d: Decidable(P)](x: Nat, y: Nat): Nat := {
  if h: (P) {
    let proofCopy := h;
    x
  } else {
    let proofCopy := h;
    y
  }
}

def forward(P: Prop)[d: Decidable(P)](x: Nat, y: Nat): Nat := {
  choose(P, x, y)
}

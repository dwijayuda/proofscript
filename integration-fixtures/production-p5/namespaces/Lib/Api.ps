module;

namespace Math {
  public def inc(x: Nat): Nat := { x + 1 }
  public def add2(x: Nat): Nat := { inc(inc(x)) }
  def hidden(x: Nat): Nat := { x + 99 }
}

namespace Extra {
  public def doubleInc(x: Nat): Nat := { Math.add2(Math.add2(x)) }
}

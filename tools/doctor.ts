import { spawnSync } from "node:child_process"; console.log(`node ${process.version}`);const t=spawnSync("tsc",["--version"],{encoding:"utf8"});console.log((t.stdout||t.stderr).trim());const l=spawnSync("lean",["--version"],{encoding:"utf8"});console.log(l.status===0?(l.stdout||l.stderr).trim():"Lean: not installed (optional for standalone ProofScript; required for oracle differential checks)");

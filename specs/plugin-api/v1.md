# Plugin API v1

Frozen MVP capabilities: `backend:v1`, `oracle:v1`.

No API is provided for changing logical kernel rules or directly mutating the trusted environment. Future syntax/macro/elaborator/tactic APIs require a separate versioned design because ProofScript must preserve Lean-style incremental parsing/elaboration semantics.

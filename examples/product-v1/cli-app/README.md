# Product-v1 CLI app

Representative command-line workflow using the canonical public CLI:

```text
psc check Main.ps
psc build-js Main.ps --out dist/Main.js
psc run Main.ps --call loyaltyPrice --args 100,true
```

Expected result: `95`.

This example exercises the supported software profile only; it does not require Lean.

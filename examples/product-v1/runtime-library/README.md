# ProofScript runtime library example

`src/Math.ps` is checked and emitted to both `dist/index.js` and
`dist/index.ts`. A normal TypeScript consumer imports the generated named
exports. The emitted TypeScript uses the bundled runtime mode for the
representative-app gate.

-- Loop verification example.
-- This is part of the verification profile, not ordinary JS syntax.

function countTo(n: Nat): Nat
  ensures done: result = n
:= {
  let mut i: Nat := 0;
  while (i < n)
    invariant inv_bound: i <= n
    decreases remaining: n - i
  {
    i := i + 1;
  }
  i
}

-- Stateful verification example.
-- The State/Bank meaning is supplied by 09-StatefulVerification.model.json.

function withdraw(from: AccountId, amount: Nat): State Bank Unit
  ensures debit: balanceOf(from) = old(balanceOf(from)) - amount
:= do {
  debit(from, amount);
}

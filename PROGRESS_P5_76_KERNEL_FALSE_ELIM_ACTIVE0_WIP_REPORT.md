# P5.76 WIP: kernel-false-elim-active0

Status: WIP / not frozen.

## Goal

Add Lean-style `False` and `False.elim` to the checked primitive prelude.

## Exact Lean 4.33.1 observation

```text
inductive False : Prop
False.elim.{u} {C : Sort u} (h : False) : C
'False.elim' does not depend on any axioms
```

## Implementation choice

- `False` is installed as the canonical empty proposition.
- `False.rec` is generated by the trusted kernel's empty-inductive machinery.
- `False.elim` is a checked reducible definition derived from `False.rec`, not a new axiom.
- The default user profile still rejects arbitrary empty inductives; the primitive installer enables empty-inductive admission only while installing the canonical `False` primitive.

## TDD evidence

RED on P5.75 baseline:

```text
SyntaxError: Named export 'falseDeclaration' not found
```

GREEN after implementation:

```text
npm run test:kernel:false-elim-active0
KERNEL_FALSE_ELIM_ACTIVE0=PASS exact-lean=PASS
```

## Verification completed so far

- `npm run build -- --pretty false`: PASS
- `npm run test:kernel:false-elim-active0`: PASS
- inherited focused Iff/Classical/Quot gates: PASS
- `npm run test:kernel:smoke`: PASS

## Known remaining before freeze

- update P5 baseline/release metadata from P5.75 to P5.76;
- run full architecture/conformance/standalone/K3-TB gates;
- run exact Lean differential;
- package final ZIP and fresh-extract validate.

Trust boundary remains K3-TB only; formal Lean 4 equivalence proven obligations remain 0.
